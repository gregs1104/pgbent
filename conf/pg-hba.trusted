# Example of setting up PG for unauthenticated access from
# any client on localhost or on a local network such as a
# 192.168.* private network.
#
# Please do not deploy like this onto an Internet exposed host.

# IPv4 local connections:
host    all             all             127.0.0.1/32            trust
host    all             all             192.168.50.1/24         trust
# IPv6 local connections:
host    all             all             ::1/128                 trust
